<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csi.mapper.RoleMapper">
    <cache/>

    <resultMap id="RoleResultMap" type="com.csi.domain.Role">
        <id property="id" column="id"/>
        <result property="roleName" column="role_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDelete" column="is_delete"/>
    </resultMap>

    <select id="findAll" resultMap="RoleResultMap">
        SELECT id, role_name, create_time, update_time, is_delete
        FROM role
        WHERE is_delete = 0
    </select>

    <select id="findById" parameterType="int" resultMap="RoleResultMap">
        SELECT id, role_name, create_time, update_time, is_delete
        FROM role
        WHERE id = #{id} AND is_delete = 0
    </select>

    <insert id="save" parameterType="com.csi.domain.Role">
        INSERT INTO role (role_name, create_time, update_time, is_delete)
        VALUES (#{roleName}, NOW(), NOW(), #{isDelete})
    </insert>

    <update id="update" parameterType="com.csi.domain.Role">
        UPDATE role
        SET role_name = #{roleName},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        UPDATE role
        SET is_delete = 1,
            update_time = NOW()
        WHERE id = #{id}
    </delete>
</mapper>
