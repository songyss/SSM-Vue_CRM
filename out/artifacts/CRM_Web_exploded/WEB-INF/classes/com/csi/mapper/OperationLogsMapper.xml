<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csi.mapper.OperationLogsMapper">
    <cache/>
    <insert id="insert">
        insert into operation_logs(operator_id,operation,target_type,target_id,old_value,new_value,ipaddress)
        values(operatorId,operation,targetType,targetId,oldValue,newValue,ipAddress)
    </insert>

</mapper>